# Boat Pro USA — Website Files Ready to Upload

Upload the **contents of this folder** to the root of the GitHub repository:

`dscottpoii/BoatProUSA.com`

Use the `main` branch. Do not upload the outer deployment-kit folder as one folder.
The repository root must contain `index.html`, `CNAME`, `assets/`, and the other page files.

The website is configured to submit newsletter forms to:

`https://script.google.com/macros/s/AKfycbwSLD7gmeiUItt2wJVozJU1AX_2NzJwrnynRCxEvA_LhcRYIxhAtXXd1Oi2tzUImpYV8A/exec`

Do not promote the signup form until the endpoint health test returns `"ok":true`.
Paid checkout is intentionally disabled until Stripe and secure member authentication are complete.
